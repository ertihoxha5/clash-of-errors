import {requirePlayer} from "../auth";
import PlatformShell from "../PlatformShell";
import s from "../platform.module.css";
import {platformData} from "../../lib/platform-data";
import RankBadge from "../components/RankBadge";
export const dynamic="force-dynamic";
export default async function Page(){await requirePlayer("/leaderboards");const d=await platformData();return <PlatformShell title="The Hall of Champions" kicker="THE REALM'S LEADERBOARD" subtitle="Current XP determines your badge and standing. Bonuses raise your score; challenge and inactivity penalties can lower it. Team totals combine current member XP."><a className="btn cyan outline" href="/rewards">Explore all 19 badges →</a><div className={s.grid}><section className={s.card}><h2>Players</h2><table className={s.table}><thead><tr><th>Player</th><th>Badge</th><th>XP</th></tr></thead><tbody>{d.leaders.map((p,i)=><tr key={i}><td>{p.display_name}</td><td><RankBadge xp={p.xp} compact/></td><td>{p.xp.toLocaleString()}</td></tr>)}</tbody></table></section><section className={s.card}><h2>Teams</h2>{d.teams.length?<table className={s.table}><thead><tr><th>Team</th><th>Members</th><th>XP</th></tr></thead><tbody>{d.teams.map((t,i)=><tr key={i}><td>{t.name}</td><td>{t.members}</td><td>{t.xp.toLocaleString()}</td></tr>)}</tbody></table>:<p>No teams yet. <a href="/teams">Create the first squad.</a></p>}</section></div></PlatformShell>}
