"use client";
import {useState} from "react";
type Topic={id:number;name:string};
export default function ArenaSetup({topics}:{topics:Topic[]}){const [code,setCode]=useState(""),[title,setTitle]=useState("Logic Legends"),[topicId,setTopicId]=useState(String(topics[0]?.id||"")),[difficulty,setDifficulty]=useState("medium"),[visibility,setVisibility]=useState("private"),[status,setStatus]=useState("");async function send(payload:Record<string,unknown>){setStatus("Connecting to arena...");const response=await fetch("/api/arena",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)}),data=await response.json() as {code?:string;error?:string};if(response.ok&&data.code)location.href=`/room/${data.code}`;else setStatus(data.error||"Arena request failed")}return <div className="arena-setup"><header><span className="kicker">MULTIPLAYER GATEWAY</span><h1>JOIN OR CREATE <em>AN ARENA.</em></h1><p>Battle. Code. Conquer.</p></header><div className="arena-choice"><section className="join-panel"><span className="kicker">JOIN ARENA</span><h2>ENTER THE FIGHT</h2><p>Use the six-character room code shared by your host.</p><label>Room code<input value={code} maxLength={6} placeholder="X7K9A2" onChange={e=>setCode(e.target.value.toUpperCase().replace(/[^A-Z0-9]/g,""))}/></label><button className="btn cyan solid" disabled={code.length!==6} onClick={()=>send({action:"join",code})}>Enter arena</button></section><section className="create-panel"><span className="kicker">CREATE ARENA</span><h2>HOST THE CLASH</h2><div className="create-form"><label>Arena title<input value={title} maxLength={40} onChange={e=>setTitle(e.target.value)}/></label><label>Topic<select value={topicId} onChange={e=>setTopicId(e.target.value)}>{topics.map(t=><option value={t.id} key={t.id}>{t.name}</option>)}</select></label><label>Difficulty<select value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option value="easy">Easy</option><option value="medium">Medium</option><option value="hard">Hard</option></select></label><label>Visibility<select value={visibility} onChange={e=>setVisibility(e.target.value)}><option value="private">Private</option><option value="public">Public</option></select></label></div><button className="btn pink solid" onClick={()=>send({title,topicId:Number(topicId),difficulty,visibility,questionCount:10,timeLimit:30,maxParticipants:8})}>Create arena</button></section></div><p className="form-status" role="status">{status}</p></div>}
